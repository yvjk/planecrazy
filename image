-- Usage Instructions:
-- 1. Build a flat screen using blocks with a 1:1 ratio (1 block per pixel) based on your desired dimensions.
-- 2. When running the script, paint a single block and select the target grid size (e.g., 51x39).
-- 3. Ensure your built block screen matches the script UI grid size; otherwise, the image will be distorted.
--Thanks to Veko and Samet for their support.
--Borrowing ideas from Nexity

loadstring(game:HttpGet('https://raw.githubusercontent.com/yvjk/core/refs/heads/main/00'))()
